{{#extend "layout"}}
  {{#content "main"}}
    <div class="gl__page-content">

      {{#each links }}
        <a target="_blank" href="{{ this.url }}" class="dev-link__wrapper">
          <div class="dev-links__link">{{ this.name }}</div>

          <img src="img/dev-links/{{ this.img }}" alt="" class="dev-links__img">
        </a>
      {{/each}}
    </div>

  {{/content}}
{{/extend}}








































